body {
  background-image: linear-gradient(180deg, blue, skyblue, grey);
  color: #fff;
 background-repeat: no-repeat;
  background-size: cover;
  font-family: 'Monaco', sans-serif;
}

#survey {
  padding: 12px;
  background-color: navy;
  display: block;
  position: center;
  opacity: 0.8;
  text-align: justify;
}
#survey-form {
  color: #FFF;
  opacity: 1;
  font-size: 18px;
}

input, #radio-section, #cropdown-section, select, #location-section, #comment-section, #survey, #checkbox {
  margin-bottom: 5px;
}

button{
    
}

footer {
  font-family: 'Lucida Handwriting', 'Monaco', sans-serif;
  font-weight: bold;
  padding: 15px;
  color: black;
  
}
